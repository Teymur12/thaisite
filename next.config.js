/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath və assetPrefix SILIN və ya boş buraxın
};

module.exports = nextConfig;
